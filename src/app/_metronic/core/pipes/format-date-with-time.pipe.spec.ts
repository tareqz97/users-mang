import { FormatDateWithTimePipe } from './format-date-with-time.pipe';

describe('FormatDateWithTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateWithTimePipe();
    expect(pipe).toBeTruthy();
  });
});
